import { styled } from "styled-components";

export const TitleCart = styled.h4`
  color: var(--grey-600);

  font-weight: 700;
  font-size: 18px;
  line-height: 24px;
`;

export const TextButtonCart = styled.button`
  color: var(--grey-300);

  font-weight: 600;
  font-size: 14px;
  line-height: 24px;
`;

export const ValueCart = styled.p`
  color: var(--grey-300);

  font-weight: 600;
  font-size: 14px;
  line-height: 24px;
`;
