import styled from "styled-components";

export const StyledTotalPrice = styled.footer`
  .buttonFooter {
    cursor: pointer;
  }

  .buttonFooter:hover {
    background-color: var(--color-primary);
    color: var(--white);
  }
`;
